# 更新日志

## 未发布（来自 Elmeir fork 的修复与增强，供上游审阅）

> 本节是 PR 提交内容，版本号由作者发布时决定；每条改动都是一个独立提交，可单独接受或剔除。

**修复**

- **概率模式改为「原地替换」**：原做法在 `send_service.before_send` 里 abort 原文字、再另发语音。宿主一旦 abort，发送函数返回 None，麦麦的 reply 工具会把本轮判为"发送失败"并交回 planner，LLM 随即重发 → 语音与文字反复出现。现在把待发消息的文本段原地换成语音段（`modified_kwargs.message`），发送链正常走完、消息照常入库，不再触发重试；合成失败保持原文直发，不丢内容。
- **失败提示 30 秒去重**：LLM 对失败有重试倾向，此前每次重试都会再发一条失败提示、形成刷屏。现在同一会话 30 秒内只提示一次（不改变 planner 是否结束的行为）。
- **关闭「手动命令」不再连带禁用麦麦自主语音**：`command_enabled` 原被错用在 Tool 上——关掉手动命令后，麦麦每次自主调用该工具都会收到"语音功能当前已禁用"。
- **命令 pattern 加 `^` 锚定**：原 `(?<!\S)` 只保证命令前是空白或行首，"你好 /说 xx"这类句中子串也会命中（宿主对 pattern 走 search 匹配）。
- **出站钩子显式声明 30 秒超时**：`send_service.before_send` 的默认处理器超时只有 5 秒，而本钩子内要完成合成（一条回复还可能切分成多段），超时会被宿主记入插件熔断、连续超时会让本插件的钩子被停用。

**增强（都可用配置关掉）**

- **语音原文写回对话上下文**（`[behavior] sync_chat_context`，默认开；`context_prefix` 默认 `[语音]`）：语音消息本身不带文字（可见文本只有"[语音消息]"），不写回去的话麦麦下一轮不知道自己说过什么。同时给 `send.custom` 带上 `processed_plain_text`，让入库的语音消息带着文字、长期记忆能检索到。manifest 新增 `maisaka.context.append` 能力（需完整重启麦麦）。
- **冷却限流**（`[behavior] command_cooldown_seconds`，默认 0=关闭）：同一会话两次语音合成的最小间隔，防刷屏/刷费用（TTS 按字符计费）。命令被拦时回一句冷却提示；工具被拦时向 LLM 返回失败原因，并提示"冷却期内重试仍会失败、不要连续重试"。

**兼容性**

- **不抬高最低宿主版本**：`_manifest.json` 的 host `min_version` 保持上游声明的 **1.2.0**；所用钩点与能力（`chat.receive.after_process`、`send_service.before_send`、`maisaka.context.append`）经核查在 1.2.0 起即存在；本 PR 不涉及 `stop_after_execution` 等较新宿主才有的特性。

## v1.7.1 (2026-09-16)

**修复审查发现的缺陷 + 文档/配置一致性**

- 🐞 **修复 `/语音帮助` 显示的音色永远是默认值**：帮助文本读的是 `[doubao] voice`，而音色早在 v1.4.0 就迁到了 `[voice_tone]`，导致无论配什么音色都显示"小何 2.0"。现在还会一并显示 Resource ID 与当前语音翻译方式。
- 🐞 **修复"中（外）混排文本被误判为已是目标语种"**：`_looks_like_language` 原来只要文本含该语种字符就判定"无需翻译"。麦麦的中文回复常夹带日语语气词，会导致**整句中文被跳过翻译**。现改为**比例判断**（日语看「假名 vs 汉字」，其他语种看「目标语种字符 vs 汉字」），中（外）混排仍会正常翻译。
- 🐞 **加强历史配置兼容**：`emotion` 的归一化原来只认 `""`/`None`，若配置里残留其他版本的"无"写法（`无`、`无（正常语气）`）会触发 Literal 校验失败、**插件直接启动不了**。现已把 `""`/`none`/`无`/`无（正常语气）` 统一归一化为 `none`。
- 📄 `config.example.toml`：`config_version` 从 `1.5.0` 修正为当前版本；页签列表补上漏掉的「语音翻译」；目标语种注释标明**声音复刻 2.0 官方支持的语种**（日语/英语/韩语/西班牙语/印尼语/葡萄牙语），并说明德语/法语/俄语等能否合成取决于音色。
- 📄 `README.md`：版本号修正；`emotion_mode=fixed` 的段名 `[doubao] emotion` 更正为 `[voice_tone] emotion`；工具名 `speak_text` 更正为实际的 `doubao_tts_speak`；目标语种说明标注官方支持范围。
- 🔧 `_manifest.json`：**capabilities 补声明 `llm.get_available_models`**——启动日志里「可选模型任务」提示会调用该能力，官方速查表明列其为合法能力名；不声明会踩评审阻断项「使用未声明能力」。
- 📄 `_manifest.json`：简介里的语种描述改为"以音色支持范围为准"，避免把"可填写的语种写法"误解为"一定可合成"。

## v1.7.0 (2026-09-16)

**语音翻译从"开关"升级为"四种模式"（可按需自定义中外混说）**

- `[translate] enabled`（布尔开关）**替换为 `mode`（四选一下拉）**：
  | 模式 | 行为 |
  | --- | --- |
  | `不翻译`（默认） | 原样合成，中英日混排也保留 |
  | `全部翻译` | 所有回复翻成目标语种（全说外语） |
  | `按概率翻译` | 每条回复按 `probability` 掷骰子 → **外语/中文交替出现** |
  | `由麦麦判断` | LLM 读这条回复自己决定翻不翻，判断依据可写在 `rule` |
- 新增字段：`probability`（翻译概率 0~1，默认 0.5，仅按概率模式生效）、`rule`（判断依据，仅麦麦判断模式生效，留空用默认规则：口语内容翻译、数字/链接/代码/专有名词保留）。
- 「由麦麦判断」为**一次调用完成"判断+翻译"**：不需要翻译时模型只输出 `KEEP`，不额外多花一次调用。
- `_translate_if_needed` 重构：模式判断 → 已是目标语种则跳过 → 概率掷骰 → 翻译/判断；全部路径失败一律回退原文。
- 启动日志升级：显示「翻译方式 / 目标语种 / 模型任务」（按概率模式额外显示概率）。
- ⚠️ 字段名变更：旧配置的 `enabled = true/false` 不再读取，请改用 `mode`（本地配置已同步迁移）。
- 版本号 1.7.0。

## v1.6.0 (2026-09-16)

**新增：语音翻译（开关式，默认关闭）——解决"麦麦说中文、音色是外语"的错位问题**

- 新增「**语音翻译**」配置页签（`[translate]`）：
  | 字段 | 说明 |
  | --- | --- |
  | `enabled` | 开关，**默认 false**（不需要的人完全无感） |
  | `target` | 目标语种 = 音色训练语种，支持中文名或代码（日语/ja、英语/en、韩语/ko、德语/de、法语/fr、西班牙语/es-mx、印尼语/id、葡萄牙语/pt-br、俄语/ru、泰语/th、阿拉伯语/ar），留空=不翻译 |
  | `model_task` | 用哪个麦麦模型任务翻译，默认 `utils`（小模型，便宜快） |
  | `max_tokens` | 译文上限，默认 800，并按原文长度自动放大（上限 4000） |
- 流程：`麦麦回复(中文) → 翻译成目标语种 → 豆包合成 → 发出语音`。
- 贴心处理：文本已是目标语种 → **跳过翻译**；翻译失败/超时/返回空 → **改用原文合成，不阻塞语音**；目标语种未填 → 记日志并跳过。
- 与 v1.5.0 的跨语种逻辑**自动衔接**：译文是日语时会自动附带 `explicit_language=ja`，发音保持正常。
- 启动日志新增：翻译开关状态 + 「可选模型任务」列表（方便填对 `model_task`）。
- ⚠️ **manifest 新增 `llm.generate` 能力** → 升级后**必须完整重启麦麦**（manifest 变更不支持热重载）。
- 验证：语种解析（中/英/日/德/韩/大小写/空/未知）、5 种翻译路径、3 种失败降级、tokens 自适应（500 字→1310）全部通过；6 个配置页签顺序正确。
- 版本号 1.6.0。

## v1.5.0 (2026-09-16)

**新增：跨语种合成（让音色说外语）**

- 复刻音色本身支持跨语种（官方《声音复刻·基本介绍》明确写有"跨语种迁移"）。
  经实测确认**具体规律**（音色 `S_RcCsDWbd2` + `seed-icl-2.0`，逐条试听对比）：

  | 文本 | 传 `explicit_language` | 听感 |
  | --- | --- | --- |
  | 日语 | ❌ 不传 | **不可接受**（发音退化） |
  | 日语 | ✅ `ja` | 正常 |
  | 中文 | 不传 | 正常 |
  | 英文 | 不传 | 正常 |

  → 结论：**中英文直接用默认即可；日语等语种必须显式指定 `explicit_language`**。

- 因此新增 `detect_language()`：按字符范围自动判断文本语种，**只在需要时**才附带 `explicit_language`
  （假名→`ja`、谚文→`ko`、西里尔→`ru`、阿拉伯→`ar`、泰文→`th`；中文/英文/拉丁语系保持不指定）。
  **零配置**：用户不需要填任何"语种"设置，麦麦说什么语言就按什么语言合成。
- 合成日志新增语种输出（`语种=ja` / `语种=默认（不指定）`），方便排查。
- 实测（走插件代码路径真实调用）：日语文本 ✓ 返回音频 53229 字节；中文文本 ✓ 正常。
- 版本号 1.5.0。

## v1.4.1 (2026-09-16)

**只修 bug，无新功能。**

- 🐞 **修复「情感 / 情感强度 / 语速 / 音量」全部不生效**：
  - 原因：这四项被写在 `req_params` **顶层**，且语速/音量用了旧接口的字段名与倍率值（`speed_ratio` / `volume_ratio`）；
  - 官方要求它们全部位于 **`req_params.audio_params`** 内，字段名为 **`speech_rate`** / **`loudness_rate`**，取值 **-50~100 的整数**（100 = 2.0 倍）；
  - 之前放在顶层会被服务端**静默忽略**（不报错），表现为"怎么调都没效果"。
  - 已实测修复后请求：`audio_params = {format, sample_rate, emotion, emotion_scale, speech_rate, loudness_rate}`，真实接口 HTTP 200 正常返回音频。
- 🐞 **修复 Markdown 符号被朗读出来**：新增 `req_params.additions = {"disable_markdown_filter": true}`。此前麦麦回复里的 `**加粗**` 会被念成"星星加粗星星"。
- 🔧 **新增音色/资源不匹配的提前告警**：当音色是复刻音色（`S_` 开头）但 Resource ID 仍是 `seed-tts-2.0`（或反之）时，日志直接给出"该改成哪个值"的提示——这是最常见的报错来源（`resource ID is mismatched with speaker related resource`）。
- 合成日志补充语速/音量输出，便于排查。
- 📄 文档：`config.example.toml` / README 的情感示例值统一为 `无（正常语气）`。
- 版本号 1.4.1。

## v1.4.0 (2026-09-03)

- 配置页按用途拆分（WebUI 页签）：**豆包语音**（API Key/Resource ID 连接）、**音色与情感**（voice/emotion/emotion_scale）、**语速与音量**（speech_rate/loudness）、**行为**（自主方式/概率/来源开关）。
- 配置段结构调整：voice/emotion/emotion_scale → `[voice_tone]`；speech_rate/loudness → `[speed_loud]`；api_key/resource_id 仍留 `[doubao]`（老配置的 key 不受影响）。

## v1.3.0 (2026-09-03)

- 配置界面化：`auto_voice_mode` 改为下拉三选一（llm/probability/off）；`resource_id` 保留文本框并提示常用两值，支持填任意资源 ID。
- 「麦麦自主 vs 手动固定」按参数独立：
  - **情感（emotion）**、**情感强度（emotion_scale）**：各配一个来源开关（`fixed` 用配置值 / `auto` 麦麦自主语音时由 LLM 现场挑）——它们是有明确档位的参数；
  - **语速（speech_rate）、音量（loudness）**：火山固定连续数值，**麦麦不可自主**，仅手动填写（字段已标注）。
- speak 工具参数精简为 text/emotion/emotion_scale（语速音量不进工具参数）。
- `resource_id` 增加占位提示与说明。

## v1.2.0 (2026-09-03)

- 新增「麦麦自主语音方式」开关（`[behavior] auto_voice_mode`）：`llm`（默认，麦麦自行判断何时用语音）/ `probability`（按概率偶尔语音，`auto_voice_probability` 可调，0.1=平均每 10 轮约 1 轮）/ `off`（仅手动命令）。
- 概率模式实现：入站钩子按概率掷骰打标 → 出站钩子把麦麦文字回复转成语音（不依赖 LLM 自觉）。
- `[behavior] auto_voice_probability`：概率模式的触发概率 0~1。
- 移除试验中的"关键词强制语音"设计（改纯概率，避免硬拦截误伤）。

## v1.1.0 (2026-09-03)

- 新增「麦麦自主选情感」开关（`[behavior] auto_emotion`）：
  - **开**：麦麦自主语音（Tool）时，LLM 根据对话氛围现场选情感（开心/伤心/生气/…），每次可不同；
  - **关**（默认）：一律使用 `[doubao]` 里配置的固定情感/语速/音量；
  - 手动 `/说` 命令**始终**使用固定配置（不受此开关影响）。
- Tool `doubao_tts_speak` 增加可选 `emotion` 参数（枚举），供自主情感模式使用。

## v1.0.0 (2026-09-03)

- 首个版本：豆包语音合成（火山引擎豆包语音合成大模型）。
- 新版控制台鉴权（X-Api-Key 单头），无需旧版 App ID / Access Token。
- 内置官方预置音色库（seed-tts-2.0），配置按名选用；也支持自定义/复刻音色（seed-icl-2.0）。
- 触发方式：`/说 文本` `/语音 文本` `/speak 文本` 手动命令 + LLM 可调用的语音 Tool。
- 发送：`send.custom("voice")` base64 直发，无需落盘。
- 健壮性：超时/网络/业务错误分级处理；失败可降级为文字；长文本按句切分多条发送。
